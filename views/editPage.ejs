<head>
    <meta charset="UTF-8">
    <link rel="stylesheet" href="/styles/dashboard.css">
</head>

<form class="flex" style="flex-direction: column;" action="/dashboard/home/<%= pageText._id %>" method="post">
    <label for="title">Title:</label>
    <input 
    dir="rtl"
    type="text" 
    name="title"
    id="title"
    value="<%= pageText.title %>">
    <textarea 
    dir="rtl"
    type="text"
    name="content" 
    id="content"> <%= pageText.content %></textarea>
    <button type="submit">add</button>
</form>